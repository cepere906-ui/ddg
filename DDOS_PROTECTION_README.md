# 🛡️ CDN с DDoS Защитой

Продвинутая система защиты от DDoS атак для Nginx в Docker контейнере.

## 📁 Структура проекта

```
ddg/
├── ddos_protection.py           # Модуль DDoS защиты (расширяемый)
├── cdn_with_ddos_protection.py  # Основной скрипт с интеграцией защиты
├── withoutddgdocker.py          # Старая версия (без защиты)
└── DDOS_PROTECTION_README.md    # Эта документация
```

## 🚀 Быстрый старт

### 1. Установка и запуск

```bash
# Сделай файлы исполняемыми
chmod +x ddos_protection.py cdn_with_ddos_protection.py

# Запусти от root (требуется для Docker)
sudo python3 cdn_with_ddos_protection.py
```

### 2. Настройка

Открой `cdn_with_ddos_protection.py` и измени:

```python
# Конфигурация твоего сервера
TARGET_DOMAIN = "твой_домен.com"
ORIGIN_IP = "IP_твоего_origin_сервера"
ORIGIN_PORT = 443

# Профиль DDoS защиты
DDOS_PROFILE = 'strict'  # basic, strict, performance, paranoid
```

## 🛡️ Профили защиты

### 1. **basic** - Базовая защита
- ✅ Подходит для большинства сайтов
- 10 запросов/сек на IP
- 10 одновременных соединений
- Базовая валидация запросов

```python
DDOS_PROFILE = 'basic'
```

### 2. **strict** - Строгая защита ⭐ (рекомендуется)
- ✅ Максимальная безопасность
- 5 запросов/сек на IP
- 5 одновременных соединений
- Короткие таймауты
- Строгая валидация

```python
DDOS_PROFILE = 'strict'
```

### 3. **performance** - Производительность
- ✅ Для высоконагруженных сайтов
- 50 запросов/сек на IP
- 50 одновременных соединений
- Больше размер запросов

```python
DDOS_PROFILE = 'performance'
```

### 4. **paranoid** - Параноидальная защита
- ⚠️ Очень строгая, может блокировать легитимных пользователей
- 2 запроса/сек на IP
- 3 одновременных соединения
- Минимальные таймауты
- Блокирует CLI инструменты (curl, wget)

```python
DDOS_PROFILE = 'paranoid'
```

## 🔧 Механизмы защиты

### ✅ Включено по умолчанию:

1. **Rate Limiting** - Ограничение частоты запросов
   - Защита от HTTP flood
   - Лимит на количество запросов в секунду с одного IP

2. **Connection Limiting** - Ограничение соединений
   - Защита от connection flood
   - Лимит одновременных соединений с одного IP

3. **Request Size Limiting** - Ограничение размера
   - Защита от больших запросов
   - Ограничение размера тела и заголовков

4. **Timeout Protection** - Защита от Slowloris
   - Короткие таймауты на чтение/запись
   - Защита от медленных атак

5. **Request Validation** - Валидация запросов
   - Блокировка подозрительных User-Agent
   - Блокировка опасных HTTP методов
   - Блокировка SQL инъекций, XSS, etc

6. **IP Blacklist** - Чёрный список IP
   - Ручная блокировка IP адресов
   - Интеграция с Fail2ban

## 📊 Мониторинг

### Проверка статуса защиты

```bash
# Проверка работоспособности CDN
curl https://твой_домен.com/cdn-health

# Проверка статуса DDoS защиты
curl https://твой_домен.com/ddos-status

# Просмотр логов
docker logs -f cdn-nginx-protected

# Последние заблокированные запросы (403, 429)
docker logs cdn-nginx-protected | grep -E "403|429"
```

### Логи Nginx

```bash
# Доступ к логам внутри контейнера
docker exec cdn-nginx-protected tail -f /var/log/nginx/access.log
docker exec cdn-nginx-protected tail -f /var/log/nginx/error.log
```

## 🔥 Расширенная настройка

### Добавление IP в чёрный список

Открой `cdn_with_ddos_protection.py`:

```python
# Создай кастомную конфигурацию
def _get_ddos_config(self, profile: str) -> DDoSProtectionConfig:
    config = ProtectionProfiles.strict()  # базовый профиль

    # Добавь IP в чёрный список
    config.blacklisted_ips = [
        "1.2.3.4",
        "5.6.7.8",
        "10.0.0.0/24",  # целая подсеть
    ]

    return config
```

### Кастомные правила

Открой `ddos_protection.py` и создай свой профиль:

```python
@staticmethod
def custom() -> DDoSProtectionConfig:
    """Твой кастомный профиль"""
    return DDoSProtectionConfig(
        rate_limit_requests=15,
        rate_limit_burst=30,
        conn_limit_connections=20,
        client_body_size="5m",
        # ... другие параметры
    )
```

Затем используй его:

```python
DDOS_PROFILE = 'custom'
```

### Geo-blocking (блокировка стран)

Требует установки GeoIP модуля в Nginx. Добавь в конфигурацию:

```python
config.geo_blocking_enabled = True
config.blocked_countries = ['CN', 'RU', 'KP']  # ISO коды стран
```

## 🆘 Fail2ban интеграция

### 1. Установи Fail2ban

```bash
apt update
apt install fail2ban -y
```

### 2. Создай фильтр

```bash
cat > /etc/fail2ban/filter.d/nginx-ddos.conf << 'EOF'
[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD).*" (403|429|444)
ignoreregex =
EOF
```

### 3. Добавь jail

```bash
cat >> /etc/fail2ban/jail.local << 'EOF'
[nginx-ddos]
enabled = true
port = http,https
logpath = /var/log/nginx/access.log
maxretry = 10
findtime = 60
bantime = 3600
EOF
```

### 4. Перезапусти Fail2ban

```bash
systemctl restart fail2ban
systemctl status fail2ban
```

### 5. Проверка

```bash
# Статус jail
fail2ban-client status nginx-ddos

# Список забаненных IP
fail2ban-client get nginx-ddos banned
```

## 🧪 Тестирование защиты

### Тест Rate Limiting

```bash
# Быстрые запросы - должны получить 429 (Too Many Requests)
for i in {1..100}; do
    curl -s -o /dev/null -w "%{http_code}\n" https://твой_домен.com/
done
```

### Тест Connection Limiting

```bash
# Множественные параллельные соединения
seq 1 50 | xargs -n1 -P50 curl -s https://твой_домен.com/ > /dev/null
```

### Тест блокировки подозрительных User-Agent

```bash
# Должен получить 403
curl -A "sqlmap/1.0" https://твой_домен.com/

# Должен получить 403
curl -A "" https://твой_домен.com/
```

### Тест блокировки опасных запросов

```bash
# SQL инъекция - должен получить 403
curl "https://твой_домен.com/?id=1' UNION SELECT * FROM users--"

# XSS - должен получить 403
curl "https://твой_домен.com/?q=<script>alert(1)</script>"
```

## 📈 Оптимизация

### Для сайтов с высокой нагрузкой

1. Увеличь `worker_connections` в Nginx
2. Используй профиль `performance`
3. Настрой кеширование статики
4. Добавь CDN (Cloudflare, etc)

### Для максимальной защиты

1. Используй профиль `strict` или `paranoid`
2. Включи Fail2ban
3. Добавь WAF (ModSecurity)
4. Настрой Geo-blocking
5. Используй IP whitelist для админки

## 🔄 Управление контейнером

```bash
# Перезапуск
cd /opt/cdn-docker && docker compose restart

# Остановка
cd /opt/cdn-docker && docker compose down

# Запуск
cd /opt/cdn-docker && docker compose up -d

# Обновление конфигурации
# 1. Отредактируй конфиг
nano /opt/cdn-docker/nginx/nginx.conf

# 2. Перезапусти
docker compose restart
```

## 🐛 Troubleshooting

### Проблема: Слишком много блокировок легитимных пользователей

**Решение:** Используй более мягкий профиль (basic вместо strict)

```python
DDOS_PROFILE = 'basic'
```

### Проблема: Атака всё равно проходит

**Решение:** Усиль защиту

1. Переключись на `paranoid` профиль
2. Добавь агрессивные IP в blacklist
3. Включи Fail2ban
4. Уменьши rate limiting

### Проблема: Nginx не запускается

```bash
# Проверь конфигурацию
docker exec cdn-nginx-protected nginx -t

# Смотри логи
docker logs cdn-nginx-protected
```

## 📚 Дальнейшее развитие

### Что можно добавить:

1. ✅ **WAF (Web Application Firewall)**
   - ModSecurity
   - NAXSI

2. ✅ **Advanced Rate Limiting**
   - Разные лимиты для разных URL
   - Whitelist для доверенных IP

3. ✅ **Captcha Challenge**
   - Google reCAPTCHA
   - hCaptcha

4. ✅ **Behavior Analysis**
   - Машинное обучение для обнаружения ботов
   - Анализ паттернов

5. ✅ **Distributed Protection**
   - Синхронизация между серверами
   - Shared blacklist

## 🎯 Рекомендации

### Для небольших сайтов (до 1000 посетителей/день)
```python
DDOS_PROFILE = 'basic'
```

### Для средних сайтов (1000-10000 посетителей/день)
```python
DDOS_PROFILE = 'strict'
```

### Для высоконагруженных (10000+ посетителей/день)
```python
DDOS_PROFILE = 'performance'
# + добавь Cloudflare или аналогичный CDN
```

### Если уже атакуют
```python
DDOS_PROFILE = 'paranoid'
# + включи Fail2ban
# + добавь IP атакующих в blacklist
```

## 📞 Поддержка

Если возникли проблемы:

1. Проверь логи: `docker logs cdn-nginx-protected`
2. Проверь конфиг: `docker exec cdn-nginx-protected nginx -t`
3. Убедись что порты 80/443 открыты: `netstat -tulpn | grep :80`
4. Проверь что Docker работает: `docker ps`

---

## 🔐 Безопасность

**ВАЖНО:**
- Используй только на VPS/dedicated серверах
- Регулярно обновляй Docker и Nginx
- Мониторь логи на подозрительную активность
- Делай бэкапы конфигурации
- Не публикуй приватные ключи и сертификаты

---

Создано для защиты твоего сервера от DDoS атак! 🛡️
